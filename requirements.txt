# AI Data Pipeline Security Audit
# Python dependencies

# Core (stdlib only - no external deps required for main scripts)
# All core scripts use: json, math, hashlib, statistics, collections, 
#                       dataclasses, datetime, typing, argparse, os, csv

# Optional: for enhanced reporting and visualization
matplotlib>=3.8.0
pandas>=2.1.0
tabulate>=0.9.0
rich>=13.7.0          # Pretty terminal output

# Optional: for Jupyter notebook analysis
jupyter>=1.0.0
ipykernel>=6.27.0

# Development / Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Optional: for integration with annotation platforms
requests>=2.31.0
python-dotenv>=1.0.0
